import SignupClient from './SignupClient';

export const metadata = {
  title: 'Sign Up — Saanvi',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Page() {
  return <SignupClient />;
}
