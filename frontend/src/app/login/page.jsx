import LoginClient from './LoginClient';

export const metadata = {
  title: 'Sign In — Saanvi',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Page() {
  return <LoginClient />;
}
